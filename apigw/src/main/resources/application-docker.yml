spring:
  application:
    name: apigw
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: customer
              uri: http://customer:8081
              predicates:
                - Path=/api/customers/**

server:
  port: 8085

