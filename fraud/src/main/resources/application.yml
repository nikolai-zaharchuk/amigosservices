server:
  port: 8085
spring:
  application:
    name: fraud
  datasource:
    username: admin
    password: admin
    url: jdbc:postgresql://localhost:5332/postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
